# .logs -- A Media Tracking Web App
A minimal media tracker made through learning the basics of Svelte.

This app makes use of browser `localStorage` to store memory across site refreshes, browsers closing, and even computer restarts.

Setting up a new Svelte project:
```bash
npx sv create <app-name>
cd <app-name>
npm install
npm run dev -- --open
```